@model IEnumerable<LabortaryManagementSystem.Models.TechnologiesMaster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TechnologiesName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TechnologiesName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TechnologiesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TechnologiesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TechnologiesId })
        </td>
    </tr>
}

</table>
